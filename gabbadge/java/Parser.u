class Person(protected String name,private Int age,protected String sex){

}

/*
annotations? modifiers (class|enum|interface) Identifier typeParameters?
	primaryConstructor?
   	typeConstraints
   	(classBody? | enumClassBody)
*/
class Male(name,age) : Person {
	init{

	}
}