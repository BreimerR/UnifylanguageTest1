func A(String  /(?<name>[a-zA-Z_])@(?<host>[a-zA-Z_])\.com/ email )  throw "'Invalid Email Format" {

}

func A(String email) {
    if( /(?<name>[a-zA-Z_])@(?<host>[a-zA-Z_])\.com/.test(email)){
        // then do this else
    }else{
        throw Error("Invalid Email Format");
    }
}