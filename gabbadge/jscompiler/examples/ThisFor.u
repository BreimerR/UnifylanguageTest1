class A{

    parse(){
        for($a in key:value){
            // this will provide the previous value that was available.
            // optimization of this has to be done correctly.
            log thisFor.prev;
        }
    }
}