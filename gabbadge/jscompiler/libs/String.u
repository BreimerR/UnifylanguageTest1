class String (tokens) {

	toRegex => new Regex(this,arg) ;

	operator increment{

	}

	operator decrement {

	}

	// this is an easier way to initialize the class
	// instead of using the native new String();
	operator shortcut{
		/*
			this has to be an actual token parse method
			returns a token order
		*/

		return new self(token);
	}

}