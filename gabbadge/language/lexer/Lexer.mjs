export class Lexer {
	
	
	static isnewline(c) {
		return /[\n\r]/.test(c);
	}
	
	static isdigit(c) {
		return /[0-9]/.test(c);
	}
	
	static isalpha(c) {
		return /[a-zA-Z_]/.test(c);
	}
	
	static isalphanum(c) {
		return 
	}
}
