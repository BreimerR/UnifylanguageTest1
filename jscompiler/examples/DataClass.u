data class Person(String name,Int age,String sex){

}

class P{

		private String name,
									 sex
						Int age

		init(name, age, sex){
				this.name = name
				this.sex= sex
				this.age = age
		}


		// we pass what to change here
		// we maintain what is not changed
		// if defined change else return as is
		copy(name,sex,age)

}