// some lambda expressions do not return anything
// some do a return and we need to have a differentiating factor between the two



class Object{
	infix instanceOf {
		// check if it is of same class
		// check if arg is child class

	}
}



class String{

	postFix isString{
		return arg instanceOf this;
	}
}


class Person{
	set fName && lName  => {
		if(isString arg)
	}
}